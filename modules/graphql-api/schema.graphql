scalar IsoDate
scalar IsoTime

type Query {
  loggedInUser: User!
  getRows(limit: Int!, offset: Int!): [Row!]!
}

type Row {
  id: ID!
  firstName: String!
  lastName: String!
  suffix: String!
  job: String!
}

type User {
  id: Int!
  firstName: String!
  lastName: String!
}

type Mutation {
  exampleMutation: String
}
