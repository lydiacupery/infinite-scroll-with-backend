scalar IsoDate
scalar IsoTime

type Query {
  loggedInUser: User!
  getRowPage(limit: Int!, offset: Int!): Page!
}

type Page {
  rows: [Row!]!
  pageInfo: PageInfo!
}

type PageInfo {
  hasNextRow: Boolean!
}

type Row {
  id: ID!
  index: Int!
  firstName: String!
  lastName: String!
  suffix: String!
  job: String!
}

type User {
  id: Int!
  firstName: String!
  lastName: String!
}

type Mutation {
  exampleMutation: String
}
