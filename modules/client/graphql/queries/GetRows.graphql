query GetRows($limit: Int!, $offset: Int!) {
  getRows(limit: $limit, offset: $offset) {
    rows {
      id
      firstName
      lastName
      suffix
      job
      index
    }
    hasNextRow
  }
}
