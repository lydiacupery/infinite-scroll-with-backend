query GetRowsConnection($limit: Int!, $offset: Int!) {
  getRowsConnection(limit: $limit, offset: $offset) {
    rows {
      id
      firstName
      lastName
      suffix
      job
      index
    }
    pageInfo {
      hasNextRow
    }
    totalCount
  }
}
